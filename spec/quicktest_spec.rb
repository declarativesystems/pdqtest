require "spec_helper"

describe Quicktest do
  it "has a version number" do
    expect(Quicktest::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
